通过 docker-compose.yml 实现多服务编排，backend 服务构建自 ./backend 目录，frontend 服务构建自 ./frontend 目录。两个服务通过共享网络通信，backend 依赖 postgres 和 redis 服务启动，frontend 依赖 backend 服务。setup.sh 和 restart.sh 脚本提供本地开发环境的一键部署能力，自动处理数据库初始化、依赖安装和服务启动流程。