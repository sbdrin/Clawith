- 使用 Docker Compose 定义多容器服务架构，包括 postgres、redis、backend 和 frontend 四个主要服务
- 后端服务构建自 ../backend 目录，前端服务构建自 ../frontend 目录
- 通过环境变量管理配置，支持外部依赖如 Feishu OAuth、MinIO 存储等
- Nginx 配置作为反向代理，处理静态资源缓存策略、API 代理、WebSocket 连接和大文件上传
- 服务间通过健康检查确保启动顺序，使用命名网络连接各组件