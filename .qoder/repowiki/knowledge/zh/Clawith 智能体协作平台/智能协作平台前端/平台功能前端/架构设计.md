- 使用React和TypeScript构建，采用组件化架构
- 状态管理使用Zustand库，分为认证状态(useAuthStore)和应用状态(useAppStore)
- API服务层统一处理HTTP请求和错误，包含认证令牌管理和自动登出机制
- 组件结构按功能划分：页面(pages)、可复用组件(components)、自定义Hook(hooks)、数据类型(types)、工具函数(utils)等
- 路由采用懒加载(lazy import)优化性能
- 国际化支持中英文切换(i18n)