采用React组件架构，以GroupsPage为核心页面，通过Modal组件（CreateGroupModal、GroupSettingsModal、InviteMemberModal）处理用户交互。使用React Query进行数据缓存和同步，通过groupApi服务层与后端通信。包含实时消息处理hook（useGroupRealtime）和提及绑定逻辑（mentionBindings.ts）处理消息中的用户提及功能。组件间通过props传递状态和回调函数进行通信。