通过app/main.py启动应用，各子模块通过共享的FastAPI实例(app/api/)进行路由注册，使用统一的数据库连接(app/database.py)和中间件(app/core/middleware.py)。服务层(app/services/)依赖模型层(app/models/)进行数据操作，API层通过DAO层(app/dao/)与数据库交互。Alembic迁移脚本(alembic/versions/)管理数据库模式演进，所有服务通过依赖注入获取共享资源如数据库会话、缓存和外部服务客户端。