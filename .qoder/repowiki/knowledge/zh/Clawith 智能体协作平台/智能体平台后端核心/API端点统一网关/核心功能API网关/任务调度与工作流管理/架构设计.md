模块采用FastAPI路由架构，包含三个主要API组件：tasks.py处理任务CRUD和日志记录，schedules.py管理定时任务调度，okr.py实现目标与关键成果管理。所有端点都通过统一的权限检查机制（check_agent_access）进行访问控制，并使用SQLAlchemy异步会话进行数据库操作。内部服务集成包括任务执行器(app/services/task_executor)和心跳运行时(app/services/heartbeat_runtime)，支持后台异步执行和进度跟踪。