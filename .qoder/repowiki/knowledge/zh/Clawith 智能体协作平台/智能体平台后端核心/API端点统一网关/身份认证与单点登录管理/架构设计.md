采用FastAPI路由架构，分为auth、sso、users三个API路由器。内部按职责分层：控制器层处理HTTP请求，服务层（registration_service、auth_provider_registry）封装业务逻辑，DAO层（identity_dao、user_dao）负责数据访问。使用事务管理器(transaction)确保数据一致性，Redis缓存OAuth中间状态，支持多租户身份隔离。核心实体包括全局Identity和租户级User，通过identity_id关联。