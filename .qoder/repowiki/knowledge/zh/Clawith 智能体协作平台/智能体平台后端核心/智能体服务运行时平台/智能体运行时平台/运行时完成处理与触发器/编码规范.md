- 使用统一的处理器接口模式，每个处理器都有handle方法处理RuntimeRunRecord和CheckpointObservation
- 通过source_type字段区分不同类型的运行时完成处理逻辑
- 使用uuid.uuid5基于运行ID和检查点ID生成确定性的唯一标识符
- 采用frozenset定义常量集合用于状态验证
- 使用自定义异常类继承RuntimeError并包含错误代码属性