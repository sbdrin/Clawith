- 错误处理采用继承RuntimeError的自定义异常类，包含code和message字段
- 数据传输对象使用dataclass并设置frozen=True和slots=True以提高性能
- 异步方法统一使用async/await模式，类型注解明确标注返回值为Awaitable
- 状态转换通过TypedDict定义结构化状态对象，确保类型安全
- 配置和标识符通过Settings单例获取，使用RuntimeGraphIdentity管理图版本