该模块采用分层架构，包含四个核心组件：chat_intake.py负责事务范围内的聊天输入处理和命令入队；chat_stream.py提供将运行时事件映射到Web聊天数据包的功能；event_stream.py实现基于数据库的运行时事件轮询流；channel_chat.py处理非Web聊天通道的运行时输入。各组件通过统一的契约类（RunHandle、RuntimeEvent等）进行交互，使用SQLAlchemy异步会话进行数据持久化，并通过RunStateReader读取检查点状态。