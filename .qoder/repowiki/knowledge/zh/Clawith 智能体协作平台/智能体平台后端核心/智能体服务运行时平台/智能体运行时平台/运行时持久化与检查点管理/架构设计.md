该模块采用分层架构设计：
- persistence.py 提供核心数据持久化逻辑，包含运行注册、命令队列管理和事务处理
- checkpointer.py 负责LangGraph检查点序列化和数据库连接配置
- run_state_reader.py 实现运行状态读取，整合命令和检查点数据生成统一视图
内部使用SQLAlchemy异步会话进行数据库操作，通过dataclass定义数据结构，实现命令的幂等性保证和调度队列控制。