该模块采用分层架构设计：
- 核心层：RuntimeCommandWorker负责命令的声明式处理，包括获取、执行、应用和结算
- 协调层：提供ThreadLock机制确保同一时间只有一个工作器处理特定线程的命令
- 防护层：AgentCycleGuard防止智能体委托循环超过配置限制
- 可见性层：thread_visibility控制模型可见的消息边界
- 守护进程层：多个独立的守护进程（命令、通道传递、异步工具轮询、产品协调）并行运行
所有组件通过协议接口解耦，支持可插拔的实现策略