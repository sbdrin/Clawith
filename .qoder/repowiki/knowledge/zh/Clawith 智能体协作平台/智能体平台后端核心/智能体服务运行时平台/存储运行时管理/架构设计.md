采用抽象基类StorageBackend定义统一接口，通过facade.py中的工厂函数get_storage_backend根据配置选择具体实现。包含三个核心实现：S3StorageBackend（使用aioboto3异步客户端）、LocalStorageBackend（带文件锁的原子操作）和FallbackStorageBackend（读穿式回退策略）。base.py定义了存储实体、版本和条件写入的数据结构及契约。utils.py提供路径规范化工具，agent_files.py处理智能体特定的存储键生成。