- 核心模块分为：intake（事务范围内的执行摄入）、dispatch（执行分发）、executions（执行声明和完成）、queue（队列管理）、evaluator（触发器评估）
- 使用异步数据库会话（AsyncSession）进行事务管理，确保执行状态一致性
- 实现租约机制防止重复执行，支持分布式工作节点协调
- 通过idempotency_key保证幂等性，避免重复触发
- 集成统一运行时v2，支持确定性触发器上下文构建