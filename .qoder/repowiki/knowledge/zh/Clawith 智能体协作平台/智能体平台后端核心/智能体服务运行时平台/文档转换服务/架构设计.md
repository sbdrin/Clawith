模块采用分层架构，包含HTML到PDF转换（html_to_pdf.py）和HTML到PPTX转换（html_to_pptx.py）两个主要服务。核心渲染逻辑通过chrome_renderer.py实现浏览器自动化，使用Chrome DevTools Protocol进行页面布局提取和截图。PPTX生成通过pptx_renderer.py实现，支持基于DOM流和浏览器布局两种渲染模式。__init__.py作为统一入口导出转换函数。