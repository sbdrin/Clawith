该模块包含两个沙箱后端实现：DockerBackend（通过Docker容器执行代码，提供更好的隔离性）和SubprocessBackend（通过子进程在工作空间内执行代码）。两者都继承自BaseSandboxBackend抽象基类，实现了统一的execute、health_check等接口。SubprocessBackend使用bubblewrap进行文件系统隔离，并内置安全检查机制防止危险操作。模块通过__init__.py导出两个后端类供外部使用。