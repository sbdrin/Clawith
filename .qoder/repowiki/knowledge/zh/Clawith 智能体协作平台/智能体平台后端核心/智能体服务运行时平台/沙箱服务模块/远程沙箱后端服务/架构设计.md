该模块采用继承架构，所有后端实现都继承自BaseSandboxBackend基类。内部包含两个具体实现：AioSandboxBackend用于连接aio-sandbox服务，支持shell和jupyter两种执行模式；SelfHostedBackend用于连接用户自部署的沙箱服务。两个实现都使用httpx进行异步HTTP通信，并通过SandboxConfig进行配置管理。接口边界清晰定义了execute、health_check等核心方法，依赖方向从具体实现指向基础组件（base模块和config模块）。