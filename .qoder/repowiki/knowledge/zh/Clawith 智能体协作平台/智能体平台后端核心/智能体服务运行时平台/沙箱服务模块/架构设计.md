所有沙箱后端实现都继承自base.py中的BaseSandboxBackend抽象基类并实现SandboxBackend协议；registry.py作为中央注册表，在模块加载时自动注册所有内置后端类型到_BACKEND_REGISTRY字典中，通过get_sandbox_backend工厂函数根据配置动态创建具体后端实例；config.py定义了统一的SandboxConfig模型和SandboxType枚举来管理不同后端的配置参数。