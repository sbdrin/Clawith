- 后端实现通过继承BaseSandboxBackend抽象基类来保证接口一致性
- 使用Protocol定义SandboxBackend接口契约以支持运行时类型检查
- 通过中央注册表模式在registry.py中统一管理所有后端类型的实例化
- 配置模型使用Pydantic BaseModel并包含SandboxType枚举类型字段
- execute方法接收code、language、timeout等标准化参数并返回ExecutionResult对象