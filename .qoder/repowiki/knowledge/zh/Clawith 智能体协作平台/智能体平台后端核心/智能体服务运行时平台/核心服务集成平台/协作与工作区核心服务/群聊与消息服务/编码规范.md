- 使用dataclass装饰器定义不可变的数据传输对象，通常带有frozen=True和slots=True参数
- 异常类继承自RuntimeError并包含code属性用于错误分类
- 函数命名采用下划线分隔的动词+名词形式，如create_group、list_group_sessions
- 数据库查询使用SQLAlchemy的异步会话和select语句构建复杂的过滤条件
- 时间戳统一使用UTC时区并通过datetime.now(UTC)创建