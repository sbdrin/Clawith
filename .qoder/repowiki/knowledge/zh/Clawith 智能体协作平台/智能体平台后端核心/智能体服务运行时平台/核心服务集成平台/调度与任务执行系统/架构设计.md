采用分层架构设计，包含scheduler.py负责cron任务调度，task_executor.py处理任务执行，heartbeat.py实现代理心跳机制，trigger_daemon.py管理触发器执行。各组件通过共享的RuntimeCommandIntake接口与底层运行时系统交互，使用数据库事务确保调度状态一致性。内部模块间通过函数调用和共享数据结构通信，所有后台服务都以asyncio协程形式在FastAPI启动时运行。