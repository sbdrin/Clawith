该模块采用分层架构设计，包含三个核心组件：1) AgentManager（agent_manager.py）负责Docker容器生命周期管理，包括启动、停止和删除容器；2) AgentContext（agent_context.py）构建智能体的基础提示词和动态上下文数据；3) AgentDirectory（agent_directory.py）提供组织内成员和智能体的目录查询服务。各组件通过异步数据库会话和存储后端进行交互，使用统一的UUID标识符和权限验证机制。