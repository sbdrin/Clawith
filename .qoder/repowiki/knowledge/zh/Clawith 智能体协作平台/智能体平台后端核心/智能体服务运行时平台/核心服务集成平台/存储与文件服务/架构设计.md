该模块采用分层架构设计，包含storage.py作为兼容性外观层，实际存储实现在storage_runtime包中；text_extractor.py提供多种文档格式的文本提取功能；group_file_service.py实现组范围的文件操作业务逻辑。内部通过数据类定义业务对象（GroupTextFile、GroupBinaryFile等），使用统一的存储后端接口（StorageBackend）进行文件操作，并通过版本令牌机制确保并发安全。模块遵循存储路径规范化和访问控制原则，将业务路径映射到物理存储键以防止直接访问底层命名空间。